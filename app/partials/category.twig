{# View :: Partial :: Category #}

{% if post.terms('category')%}
  {% for cat in post.terms('category') %}
    <a href="{{cat.link}}" itemprop="articleSection">{{cat.name}}</a>
  {% endfor %}
{% endif %}